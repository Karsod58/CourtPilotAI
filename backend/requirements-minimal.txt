# Minimal requirements for Render free tier (512MB RAM)
# This excludes heavy ML packages

# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.25
aiosqlite>=0.19.0
alembic>=1.13.1
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# Data Validation
pydantic>=2.5.3
pydantic-settings>=2.1.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.2

# AI/LLM - Core (lightweight)
openai>=1.10.0
anthropic>=0.8.1

# LangChain (Required for AI features)
langchain>=0.1.0
langchain-core>=0.1.0
langchain-openai>=0.0.5
langchain-anthropic>=0.1.1
langchain-community>=0.0.20

# Document Processing (lightweight - NO opencv, NO sentence-transformers)
pypdf>=4.0.1
PyPDF2>=3.0.1
pdfplumber>=0.10.3
pytesseract>=0.3.10
Pillow>=10.2.0
# pdf2image>=1.17.0  # Commented out - requires poppler which is heavy

# Utilities
python-dotenv>=1.0.0
loguru>=0.7.2
aiofiles>=23.2.1
httpx>=0.26.0
requests>=2.31.0

# Data Processing (minimal)
numpy>=1.26.3

# NOTE: The following packages are EXCLUDED to fit in 512MB RAM:
# - sentence-transformers (300MB+)
# - opencv-python (200MB+)
# - faiss-cpu (100MB+)
# - pandas (50MB+)
# - pdf2image (requires poppler)
# 
# RAG features and image preprocessing will be disabled
# Basic PDF text extraction will still work
